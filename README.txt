Main.py is the main python script used to run the models. It can be used to generate images, FID and train models. It takes user input

The models are attached in the google drive link, as they are too big to be inserted in the github.
